info:
  component: SpringConfig-Server
  
server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sergycg/SpringEurekaNetflix_1.git
          
profiles:
  active: dev

